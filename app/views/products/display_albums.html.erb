<div class="clear"></div>
<h1>Albums</h1>
<% @albums.each do |a| %>
	<%= a %><br />
	<%= link_to a[:title], display_albums_url(a) %><br />
<% end %>
<% if @pictures %>
	<% @pictures.each do |p| %>
		<%= p %>
		<%= image_tag(p[:url], :size => "100x100") %>
	<% end %>
<% end %>